-if current_user.is_a?(Business)
  = nil
-else
  - unless business.customers.include?(current_user)
    = button_to 'Follow', follow_business_path(business)
  - else
    = button_to 'Unfollow', follow_business_path(business)